#include "Carte_d'identite2.h"

int main(){
carte identite;
    // crée une structure que l'on va nommer ''carte d'identité'' et on va construire autour de cette structure un programme qui permettera de saisir une carte d'identité en mode texte
    //et enregistrer ses information au format texte dans un fichier , il faut un ID un nom un prenom, une adress,e code postale, et ville , le programme pourra saisir une carte
    //l'enregistrer au format texte dans un fichier, mais également lire une ou plusieurs carte d'identité dans un fichier
    //qu'il incremente l'enregistrement , ''client'', et au suivant ''client 2''
    //la variable utiliser ne doit pas etre globale

    creation(identite);
    return 0;
}
